RSpec.describe Contracts::Stringy do
  it "has a version number" do
    expect(Contracts::Stringy::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
